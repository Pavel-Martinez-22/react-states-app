const AboutInterface = () => {
  return (
    <main className="About">
      <h2>About React Front-End Interface</h2>
      <p style={{ marginTop: "1rem" }}>
        This project showcases my ability to build a front-end interface using React. The
        application includes search functionality, dynamic routing to individual state pages, and
        the ability for users to add new fun facts to each state.
      </p>
      <p>
        Through this project, I demonstrated my understanding of reusable components, prop drilling,
        and React hooks. I implemented client-side routing to create a multi-page user experience
        and direct users to specific components based on their interactions. A controlled form was
        used to capture user input and append new data to the corresponding state. Additionally, I
        leveraged React state and rendering logic to provide real-time search functionality across
        the state data.
      </p>
    </main>
  );
};

export default AboutInterface;
